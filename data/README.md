# Data files for the SVM practical examples

This repository includes synthetic CSV datasets that mirror the examples used inside the notebook:

- `data/battery_health.csv`
- `data/fuel_theft.csv`
- `data/tyre_replacement.csv`

These files are included to make the project easier to inspect, reuse, and extend outside the notebook environment.
